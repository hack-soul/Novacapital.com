
  # Build Nova Capital Platform

  This is a code bundle for Build Nova Capital Platform. The original project is available at https://www.figma.com/design/xrr0RY86XBmyrdpaM2uuVm/Build-Nova-Capital-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  